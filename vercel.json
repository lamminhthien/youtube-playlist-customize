{
  "regions": ["sin1"],
  "buildCommand": "npm test && npm run build:css && node scripts/verify-channels.mjs && node scripts/bump-cache-version.mjs",
  "outputDirectory": ".",
  "installCommand": "npm install"
}
