{
  "name": "youtube-playlist-customize",
  "version": "1.0.0",
  "description": "LMT YouTube Playlist feed viewer (vanilla JS + ESM).",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "bump-cache": "node scripts/bump-cache-version.mjs",
    "predeploy": "node scripts/bump-cache-version.mjs",
    "deploy": "wrangler deploy",
    "deploy:vercel": "node scripts/bump-cache-version.mjs && vercel --prod",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
